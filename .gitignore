# Node / npm
node_modules/
npm-debug.log*
*.log

# Build výstupy
dist/
www/
.angular/
coverage/
*.tsbuildinfo

# Archiv stažených výherních listin.
# POZOR: není to smetí. Je to trvalý archiv surového HTML, ze kterého se dá kdykoliv
# přeparsovat bez dotazu na síť — pojistka pro případ, že Allwyn adresu změní nebo
# stránky zavře. Do gitu se nedává jen kvůli velikosti (~114 MB), ale patří do domácí
# zálohy. Verzovaný v gitu je až vyparsovaný data/vysledky.json.
# Podrobnosti: docs/data-source.md, sekce „Odolnost zdroje".
fetcher/.cache/

# Lokální konfigurace
.env
.env.*
*.local

# Android / Capacitor
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
*.apk
*.aab
*.hprof

# Podpisové klíče — nikdy do repozitáře
*.jks
*.keystore

# Editory a OS
.idea/
.vscode/
*.iml
.DS_Store
